# سجل تطوير قاعدة البيانات (Database Development History)

## [2026-09-06 00:38:11]
- **العملية / Operation:** استعلام ديناميكي آمن على جداول المخطط (Schema User Tables).
- **التفاصيل / Details:**
  - قراءة أسماء الجداول من جدول النظام `USER_TABLES` للتحقق من وجود الجدول قبل تنفيذ الاستعلام لتجنب أخطاء `ORA-00942`.
  - تطبيق استعلامات محددة بعدد الصفوف (`limit` / `ROWNUM`) على جداول أوراكل لتحسين الأداء ومنع استهلاك الذاكرة.

## [2026-09-09 00:24:21]
- **العملية / Operation:** تحسين استعلامات كشف الحساب لجداول حركات اليومية `JOURNAL_ENTRY_REC` وحسابات العمال (`TAILOR_EMPLOYEES`, `CUTTER`, `TAILOR`).
- **التفاصيل / Details:**
  - معالجة استرجاع الحقول بحروف صغيرة أو كبيرة من محرك Oracle PDO في استعلامات العمال.
  - تطبيق دالة `TO_DATE(..., 'DD/MM/YYYY')` مع `TRUNC(ENTRY_DATE)` لضمان مطابقة التاريخ الزمني بدقة متناهية.
  - احتساب أحدث تاريخ حركة `MAX(TRUNC(ENTRY_DATE))` لتحديد أول 30 يوماً افتراضياً تلقائياً، واستخراج مجموع المدين والدائن لما قبل الفترة لتحديد الرصيد الافتتاحي بدقة.

